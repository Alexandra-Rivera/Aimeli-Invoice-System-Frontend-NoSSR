export interface ProductoInfoCompraproducto {
    idCompra: number, 
    numeroFactura: string, 
    fechaCompra: string,
    proveedor: string, 
    productos: string, 
    metodoPago: string, 
    total: number
}
